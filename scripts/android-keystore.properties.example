# Copy to android/keystore.properties (gitignored) after generating a release keystore.
# Paths are relative to android/app/ (Gradle app module).
storeFile=../task-manager-release.keystore
storePassword=CHANGE_ME
keyPassword=CHANGE_ME
keyAlias=taskmanager
